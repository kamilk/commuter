<% if user_signed_in? %>
  <div class="navbar navbar-inverse">
    <div class="navbar-inner">
      <%= link_to 'Commuter', root_path, class: 'brand' %>
      <ul class='nav'>
        <li><%= link_to 'Fuel reports', fuel_reports_path %></li>
        <li><%= link_to 'Debts', debts_path %></li>
      </ul>
      <ul class="nav pull-right">
        <li class="dropdownt">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <%= h current_user.name %> 
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
              <%= link_to 'Edit your account', edit_user_registration_path, method: :delete %>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
<% end %>
